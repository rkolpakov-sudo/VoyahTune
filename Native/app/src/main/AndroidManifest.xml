<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Чтение метаданных ЛЮБОЙ активной медиа-сессии (сторонние плееры) через MediaSessionManager.
         signature|privileged → нужен privapp-whitelist (см. privapp-permissions-ru.big.town.anative.xml). -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <!-- Перенос приложения между сплитом/фриформом БЕЗ убийства процесса: завершаем задачу приложения
         (removeTask), а не force-stop процесса — плейбек в foreground-сервисе выживает (музыка не глохнет).
         REAL_GET_TASKS — перечислить задачи всех приложений; REMOVE_TASKS — завершить чужую задачу.
         OEM REMOVE_TASKS = signature|documenter: Full использует UID-scoped ATMS.removeTask hook. -->
    <uses-permission android:name="android.permission.REAL_GET_TASKS" />
    <uses-permission android:name="android.permission.REMOVE_TASKS" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.REBOOT" />
    <uses-permission android:name="android.permission.MANAGE_APP_OPS_MODES" />
    <uses-permission android:name="android.permission.FORCE_STOP_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.car.permission.CAR_SPEED" />
    <uses-permission android:name="android.car.permission.CAR_POWER"/>
    <uses-permission android:name="android.car.permission.CAR_MILEAGE" />
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_CLIMATE" />
    <uses-permission android:name="android.car.permission.CAR_MOCK_VEHICLE_HAL" />
    <uses-permission android:name="android.car.permission.CAR_CAMERA" />
    <uses-permission android:name="android.car.permission.CAR_NAVIGATION_MANAGER" />
    <uses-permission android:name="android.car.permission.CAR_CONTROL_AUDIO_VOLUME" />
    <uses-permission android:name="android.car.permission.VEHICLE_DYNAMICS_STATE" />
    <uses-permission android:name="android.car.permission.CAR_DISPLAY_IN_CLUSTER" />
    <uses-permission android:name="android.car.permission.CAR_INSTRUMENT_CLUSTER_CONTROL" />
    <uses-permission android:name="android.car.permission.STORAGE_MONITORING" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.MANAGE_USB" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CONNECTIVITY_USE_RESTRICTED_NETWORKS" />
    <uses-permission android:name="android.permission.INJECT_EVENTS" />
    <uses-permission
        android:name="android.permission.ADD_TRUSTED_DISPLAY"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.car.permission.CAR_PROPERTY" />

    <permission
        android:name="ru.big.town.anative.permission.BIND_SET_MODES_SERVICE"
        android:protectionLevel="signature" />
    <uses-permission android:name="ru.big.town.anative.permission.BIND_SET_MODES_SERVICE" />

    <!--
      Native transports use the pinned CanBus Binder ABI. The vendor service checks this permission
      name but does not define it itself, so Native owns a signature-level definition. Installers
      reject any pre-existing foreign owner before writing the system APK.
    -->
    <permission
        android:name="com.qinggan.permission.WRITE_CANBUS"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.qinggan.permission.WRITE_CANBUS" />

    <queries>
        <provider android:authorities="ru.big.town.restoremode.restoremodecontentprovider" />
        <package android:name="com.qinggan.carsignal.service" />
        <package android:name="com.qinggan.canbus.service" />
    </queries>

    <uses-permission android:name="android.permission.CAR_POWER" />

    <uses-feature android:name="android.hardware.type.car" />
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />

    <uses-permission android:name="android.car.permission.READ_CAR_POWER_POLICY" />
    <uses-permission android:name="android.car.permission.CAR_EXTERIOR_LIGHTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.DEVICE_POWER"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission
        android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.START_ACTIVITIES_FROM_BACKGROUND"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.QUICKBOOT_POWERON" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_START_FOREGROUND_SERVICES_FROM_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_RUN_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.SCREEN_LOCKED" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.POWER_SERVICE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true" />

    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />

    <application
        android:name=".Anative"
        android:allowBackup="false"
        android:appCategory="audio"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Native"
        tools:targetApi="31">
        <!-- Экспорт без permission: STEER/OPEN_* шлёт Frida (keymanager/launcher, чужая подпись),
             KEYCODE_SWC/SCREEN/GARAGE — система. Residual risk задокументирован (H2). -->
        <receiver
            android:name=".SetModesReceiverDynamic"
            android:enabled="true"
            android:exported="true" />

        <receiver android:name=".WidgetActionReceiver" android:exported="false" />
        <!-- Удалены: RunningAppsWidget, MediaVolumeWidget — не работают, будут переделаны позже -->
        <receiver android:name=".LaunchAppsWidget" android:exported="false">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_launch_apps_info" />
        </receiver>

        <receiver
            android:name=".SetModesConfigReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="ru.big.town.anative.permission.BIND_SET_MODES_SERVICE" />

        <!-- БЕЗ directBootAware: он делал пакет PARTIALLY_DIRECT_BOOT_AWARE и ломал создание
             DE-каталога (/data/user_de/0/...) при boot-скане /system priv-app на этой ROM →
             zygote-краш "Failed to mount data_de/null". Сервис стартует по BOOT_COMPLETED
             (уже после разблокировки), direct-boot ему не нужен. -->
        <service
            android:name=".SetModesService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="shortService"
            android:permission="ru.big.town.anative.permission.BIND_SET_MODES_SERVICE" />

        <service
            android:name=".LightSensorService"
            android:enabled="true"
            android:exported="false"
            />

        <service
            android:name=".WiperColdService"
            android:enabled="true"
            android:exported="false"
            />

        <service
            android:name=".TripStatsService"
            android:enabled="true"
            android:exported="false"
            />

        <service
            android:name=".BatteryHeatService"
            android:enabled="true"
            android:exported="false"
            />

        <service
            android:name=".NowPlayingService"
            android:enabled="true"
            android:exported="false"
            />

        <service
            android:name=".BackButtonService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/back_button_accessibility_config" />
        </service>

        <receiver
            android:name=".SetModesReceiverStatic"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
<!--                <action android:name="android.intent.action.ACTION_PREFERRED_ACTIVITY_CHANGED" />-->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
<!--                <action android:name="android.intent.action.QUICKBOOT_POWERON" />-->
<!--                <action android:name="android.intent.action.BOOT" />-->
<!--                <action android:name="android.intent.action.REBOOT" />-->
<!--                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />-->
<!--                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />-->
<!--                <action android:name="android.intent.action.ACTION_SHUTDOWN" />-->
<!--                <action android:name="android.intent.action.QUICKBOOT_POWEROFF" />-->
<!--                <action android:name="android.intent.action.USER_UNLOCKED" />-->
                <action android:name="com.qinggan.intent.QINGGAN_BOOT_COMPLETE" />
<!--                <action android:name="com.android.internal.location.ALARM_WAKEUP" />-->
<!--                <action android:name="android.os.action.POWER_SAVE_MODE_CHANGED" />-->
<!--                <action android:name="android.intent.action.SCREEN_ON" />-->
<!--                <action android:name="android.intent.action.SCREEN_OFF" />-->
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="ru.big.town.anative.MainActivity" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Удалён RunningAppsActivity — связан с удалённым RunningAppsWidget -->

        <!-- Однопанельный/двухпанельный VirtualDisplay-хост (per-app DPI + живой ресайз) -->
        <activity
            android:name=".SplitHostActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:resizeableActivity="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|density" />

        <activity
            android:name=".ClusterMediaHostActivity"
            android:exported="false"
            android:taskAffinity="ru.big.town.anative.cluster"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|density" />

        <!-- «Сейчас играет» — снимок трека + обложка для наших поверхностей (читает RestoreMode/VoyahTune) -->
        <provider
            android:name=".NowPlayingProvider"
            android:authorities="ru.big.town.anative.nowplaying"
            android:exported="true"
            android:grantUriPermissions="true" />

        <!-- Шеринг лог-файла (кнопка «Выгрузить логи») -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="ru.big.town.anative.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
