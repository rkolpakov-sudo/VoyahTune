<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Основное расположение: app-private internal (files/) -->
    <files-path name="int_files" path="." />
    <!-- Запасное: app-specific external -->
    <external-files-path name="ext_files" path="." />
    <!-- Legacy: /sdcard/tmp/... (старые логи до смены path) -->
    <external-path name="ext_tmp" path="tmp/" />
</paths>
